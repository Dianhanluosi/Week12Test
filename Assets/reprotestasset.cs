using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class reprotestasset : MonoBehaviour
{

    public bool x;

    // Start is called before the first frame update
    void Start()
    {
        //bababooey
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
